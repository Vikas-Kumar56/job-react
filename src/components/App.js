import React from 'react';
import Layout from './layout';
import JobContainer from './job/JobContainer';

const App = () => {
  return (
    <Layout>
      <JobContainer />
    </Layout>
  );
};

export default App;
